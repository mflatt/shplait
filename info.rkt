#lang setup/infotab

(define collection "shplait")

(define deps '("base"
               "rhombus-prototype"))

(define build-deps '())

(define version "0.1")

(define scribblings '(("scribblings/shplait.scrbl" (multi-page) (language))))
